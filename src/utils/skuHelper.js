export const normalizeSkuTree = (maps, skuTree) => {}
export const initOpenWay = (openWay) => {}
export const isAllSelected = (skuTree, selectedSku) => {}

export default {
  normalizeSkuTree,
  isAllSelected
}
